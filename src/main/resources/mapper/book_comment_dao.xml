<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="fzz.wsback.dao.BookCommentDao">

    <resultMap type="fzz.wsback.domain.BookComment"
               id="BookCommentResultMap">
        <id column="user_id" property="userId" jdbcType="INTEGER"/>
        <id column="book_id" property="bookId" jdbcType="INTEGER"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 通过书籍 ID 获取相关的评论信息 -->
    <select id="getBookCommentListByBookId" resultMap="BookCommentResultMap" parameterType="fzz.wsback.domain.BookComment">
        select *
        from book_comment
        where book_id = #{bookId}
    </select>
</mapper>
